const DB_NAME = 'car_servise';
const SERVER_PORT = 3100;

const TOKEN_SECRET = 'some token secret key';
const TOKEN_EXPIRES_IN = '7d';

module.exports = {
  DB_NAME,
  SERVER_PORT,
  TOKEN_SECRET,
  TOKEN_EXPIRES_IN
}